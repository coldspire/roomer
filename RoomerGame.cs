
namespace Roomer
{
    class RoomerGame
    {    
        public RoomerGame()
        {
        }
        
        public void Play()
        {
            //// Gameplay loop.
            // 1) Decide what to do with user input, if any exists
            // 2) Process events based on any PC input from previous loop
            // 3a) Save/load/stop game if PC requests it
            // 3b) Start/stop events (sound, output text/graphics, etc)   
            // 4) Process events based on room
            // 4a) Start/stop events
            // 5) Process other events (e.g. timed events)
            // 6) Update state of room, PC, NPC objects 
            // 7) Get new input from user
        }
    }    
}